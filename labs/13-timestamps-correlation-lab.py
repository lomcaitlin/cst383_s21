import numpy as npimport pandas as pdimport seaborn as snsfrom matplotlib import rcParams# allow output to span multiple output lines in the consolepd.set_option('display.max_columns', 500)# switch to seaborn default stylistic parameters# see the useful https://seaborn.pydata.org/tutorial/aesthetics.htmlsns.set()sns.set_context('paper')   # 'talk' for slightly larger# change default plot sizercParams['figure.figsize'] = 9,7# 1 read datadf = pd.read_csv("https://archive.ics.uci.edu/ml/machine-learning-databases/wine-quality/winequality-red.csv", sep=";")# 2 computer correlation coefficient for dfcorr = df.corr()# 3 which features most positively correlated# 4 display correlations with plotsns.heatmap(corr)# 5 which features are most correlated to 'quality'alcohol, sulphates, and citric acid# 6 is the wine data tidy?yes?